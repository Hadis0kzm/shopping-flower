import React from 'react';

import Cards from './cards';
import Slider from './Banner';

const Landing = () => {
    return (
        <div>
            <Slider/>
            <Cards/>
        </div>
    );
};

export default Landing;